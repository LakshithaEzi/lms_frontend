export interface Book {
  id: string;
  name: string;
  author: string;
  description: string;
}

export interface BookFormData {
  name: string;
  author: string;
  description: string;
}
